pyyaml
numpy

# PyTorch inference vertical slice
torch
torchvision

# YOLOv8 object-detection vertical slice (also downloads yolov8n.pt, ~6MB,
# on first use)
ultralytics

# ImageNet dataset: parses the devkit's meta.mat for the class ID -> name
# mapping
scipy

# SAM (Segment Anything) vertical slice. Pure Python/PyTorch wheel, no
# compiled extensions, no checkpoint download needed -- built random-init
# via sam_model_registry["vit_b"](checkpoint=None).
segment-anything

# BERT/Sentiment Analysis/IMDB vertical slice. Only the tokenizer's fixed
# vocabulary is downloaded, not pretrained model weights -- the model
# itself is built from a random-init BertConfig. Deliberately NOT using
# HuggingFace's `datasets` pip package for IMDB: its own top-level import
# name collides with this project's own datasets/ package (confirmed
# directly -- see datasets/imdb.py's docstring), so IMDB is downloaded and
# parsed with the standard library instead.
transformers
pillow

# FastChat: wired into GPT-2's dispatch (llm_framework: FastChat) -- loads
# the same random-init model back through FastChat's own load_model()
# instead of keeping it in-memory. Needed accelerate too, not pulled in
# automatically by fastchat's own dependency list -- confirmed directly.
fschat
accelerate

# SpeechBrain: wired into Whisper's dispatch (speech_framework: SpeechBrain)
# -- uses speechbrain.nnet.CNN/RNN/linear directly, NOT
# speechbrain.lobes.models.CRDNN, which hard-fails here (its class
# definition triggers a broken lazy-import of an unrelated optional k2_fsa
# decoding backend -- confirmed directly, see
# speech_frameworks/speechbrain_adapter.py's docstring).
speechbrain

# llama.cpp: wired into GPT-2's dispatch (llm_framework: llama.cpp) --
# genuinely different from FastChat's dispatch above: runs real llama.cpp
# C++ inference via a real (~1.2MB) trained test GGUF model, downloaded
# once to ./data/, decode/re-encode translating at the GPT-2-tokenizer
# boundary -- see llm_frameworks/llamacpp_adapter.py's docstring.
# llama-cpp-python has no prebuilt wheel for this Python/platform and
# builds from source: needs a real C/C++ compiler (this project's own dev
# machine needed Visual Studio Build Tools -- "Desktop development with
# C++" workload -- installed specifically to unblock this).
llama-cpp-python

# ESPnet: wired into Whisper's dispatch (speech_framework: ESPnet) -- uses
# espnet2.asr.encoder.transformer_encoder.TransformerEncoder directly (a
# real ESPnet transformer encoder building block), not the full
# ESPnetASRModel/task-recipe machinery. Previously blocked on a
# sentencepiece dependency needing a C/C++ compiler to build from source;
# resolved by installing one (Visual Studio Build Tools) -- see
# speech_frameworks/espnet_adapter.py's docstring.
espnet

# Detectron2: a new architecture entry (architectures/detectron2.py),
# real random-init GeneralizedRCNN (Faster R-CNN, ResNet18 backbone) built
# directly via detectron2.modeling.build_model -- no model_zoo config/
# checkpoint needed. Never published to PyPI; install from source:
#   pip install git+https://github.com/facebookresearch/detectron2.git
# Needs a real C/C++ compiler (this project's own dev machine needed
# Visual Studio Build Tools) to compile its custom ops -- CPU-only build
# works fine without a CUDA toolkit, see architectures/detectron2.py's
# docstring.
detectron2 @ git+https://github.com/facebookresearch/detectron2.git

# Federated learning vertical slice (Flower)
flwr

# FedLab: installs and connects cleanly, but the client/server pair hangs
# after connecting -- see frameworks/fedlab_adapter.py's docstring before
# relying on it.
fedlab

# NVFlare: `import nvflare` fails outright on Windows (needs the POSIX-only
# `resource` module -- Linux/macOS only), and its simple simulator_run() API
# produces no real network traffic even where it does run -- see
# fl_frameworks/nvflare_adapter.py's docstring before relying on it.
nvflare

# FairScale distributed-training slice (OSS + ShardedDataParallel) --
# pure PyTorch extension, no special hardware needed.
fairscale

# DeepSpeed distributed-training slice (ZeRO stage 1). Blocked on Windows
# (op-builder produces zero compilable sources for a Windows/no-CUDA
# target); on Linux, plain pip install works and it JIT-builds ops via
# ninja on first use -- see distributed_frameworks/deepspeed_adapter.py's
# docstring.
deepspeed

# Ray Train distributed-training slice (TorchTrainer). Had no wheel at all
# for this project's Python version on the original Windows dev machine;
# now installs cleanly -- see distributed_frameworks/ray_train_adapter.py's
# docstring.
ray[train]

# Packet capture (requires Npcap on Windows / libpcap on Linux & macOS,
# plus admin/root privileges to actually capture -- see README.md)
scapy

# TLS transport: auto-generates a self-signed dev cert
cryptography

# transport/grpc.py is currently a stub, but Flower itself depends on grpcio
grpcio

# ONNX export (used by frameworks/onnx_runtime_adapter.py and
# frameworks/tensorrt_adapter.py, both of which go through torch.onnx.export).
# onnxscript is needed by current PyTorch's ONNX exporter machinery even
# when forcing the legacy exporter path (dynamo=False).
onnxscript

# ONNX Runtime inference. Pure Python/C++ wheel, no special hardware needed
# -- runs on any platform ONNX Runtime ships a wheel for.
onnxruntime

# OpenVINO inference. Unlike TensorRT, this is a normal pip install and
# its CPU plugin runs on any x86/ARM machine, no special hardware needed.
openvino

# TensorFlow Lite inference. `ai-edge-litert` is the lightweight interpreter
# (no full TensorFlow needed) for actually running a .tflite model; getting
# a .tflite in the first place needs `litert-torch` (formerly `ai-edge-torch`)
# to convert from PyTorch, which pulls in full `tensorflow` -- and neither
# has a wheel for every Python version yet (check before assuming this
# installs cleanly on a brand-new Python release).
ai-edge-litert
litert-torch

# TensorRT inference (Jetson only -- needs an NVIDIA GPU). NOT a plain pip
# install on most machines: on Jetson, `tensorrt` ships as a system package
# via JetPack, so build your venv with `--system-site-packages` instead of
# `pip install tensorrt`. `pycuda` is a normal pip install once CUDA is
# present. Leave these commented out unless you're actually on Jetson.
# pycuda

# ExecuTorch inference. Pure pip install where it has a wheel at all, but
# (as of this writing) it doesn't ship one for every recent Python version
# -- check `pip index versions executorch` before assuming it installs
# cleanly on a brand-new Python release, same caveat as ai-edge-litert/
# litert-torch above.
executorch

# TVM inference. `apache-tvm` has no prebuilt wheel for every platform --
# where none exists, pip tries to build its `apache-tvm-ffi` component
# from source via CMake, which needs a working C/C++ compiler toolchain
# installed and on PATH. Check `pip install apache-tvm` succeeds before
# assuming this works; if it tries to compile from source, either install
# a compiler toolchain or skip this one.
apache-tvm

# CoreML inference. Plain pip install anywhere, no compiler needed --
# conversion (coremltools.convert) works on any platform. Actually
# *running* the converted model (MLModel.predict()) only works on
# macOS/iOS -- confirmed on this project's own Windows dev machine, it
# raises a clean "Model prediction is only supported on macOS" error, the
# same shape as TensorRT needing an NVIDIA GPU.
coremltools

# Karate Club dataset (GCN/Node Classification vertical slice) -- loads
# the real graph via networkx.karate_club_graph().
networkx

# PyTorch Geometric: real, verified GCNConv-based GCN, equivalent to
# architectures/gcn.py's hand-rolled version -- see
# graph_frameworks/pytorch_geometric_adapter.py's docstring, including the
# honest disclosure that graph_framework isn't actually wired into
# execution yet. Pure-Python wheel, no platform-specific build.
torch_geometric

# Diffusers: real, verified UNet2DModel/DDPMScheduler-based DDPM sampler,
# equivalent to architectures/ddpm.py's hand-rolled one -- see
# diffusion_frameworks/diffusers_adapter.py's docstring, same honest
# disclosure as PyTorch Geometric above (diffusion_framework isn't wired
# into execution yet). Pure-Python wheel.
diffusers

# --- Everything below is written to its framework's documented API but
# NOT execution-verified in this project's environment -- each one was
# checked against real pip/wheel availability first and landed here
# specifically because that check failed. See README.md's framework list
# and each frameworks/*_adapter.py docstring for the specific blocker.

# TensorFlow inference (via onnx2tf conversion). Now installs cleanly on
# Linux + Python 3.13 (tensorflow ships a real cp313 wheel as of 2.21.0) --
# check `pip index versions tensorflow` yourself before assuming this on a
# different Python version. onnx2tf's own PyPI metadata under-declares its
# dependencies (needs tf_keras, onnx_graphsurgeon, ai-edge-litert, and
# sng4onnx too, none pulled in automatically), and the version this
# project's dependency resolution settles on (1.28.8) has a real bug
# loading its bundled calibration image under numpy 2.x's stricter
# allow_pickle default -- see frameworks/tensorflow_adapter.py's docstring
# before assuming this runs end-to-end.
tensorflow
onnx2tf
tf_keras
onnx_graphsurgeon
sng4onnx

# TensorFlow Lite Micro inference. Not a PyPI package at all -- there is
# no `pip install` path, only a from-source Bazel build against
# github.com/tensorflow/tflite-micro. Left commented out; this isn't
# something `pip install -r requirements.txt` can ever satisfy as-is.
# tflite-micro (not on PyPI -- build from source)

# MediaPipe inference. Installs cleanly (confirmed) -- what's blocked is
# the TensorFlow Lite conversion step above, not mediapipe itself.
mediapipe

# NCNN inference (via pnnx conversion). Both are normal pip installs, but
# pnnx.export() runs a compiled binary bundled in its wheel via
# subprocess -- deliberately not exercised in this project without an
# explicit decision to do so each time. Uncomment only if you've reviewed
# what pnnx's bundled binary does.
# ncnn
# pnnx

# MNN inference (Alibaba). Normal pip install, but its bundled
# mnnconvert.exe has confirmed, undisclosed pip-install-on-`--help`
# behavior in this project (tried `mnnconvert --help` directly: instead
# of printing usage, it silently started `pip install
# aliyun-log-python-sdk` and a chain of unrelated network dependencies --
# no --help output at all). Nothing from that completed here, and mnn has
# been uninstalled from this project's environment as a precaution. See
# frameworks/mnn_adapter.py's docstring before installing this or running
# its conversion step -- deliberately left unexercised.
# mnn

# SNPE inference (Qualcomm). Not a pip package -- requires manually
# downloading the Qualcomm Neural Processing SDK (free developer account)
# from https://qpm.qualcomm.com/ and setting SNPE_ROOT.

# RKNN inference (Rockchip). `rknn-toolkit2` has no wheel for this
# platform/Python version (confirmed via `pip index versions
# rknn-toolkit2`) -- Rockchip only publishes for specific Linux + Python
# combinations.
# rknn-toolkit2

# Arm NN inference. `pyarmnn` isn't a normal PyPI package on x86_64
# Windows -- get prebuilt wheels matched to a specific Arm NN release from
# github.com/ARM-software/armnn/releases, or build from source, targeting
# an actual Arm platform (Android, Arm Linux, Jetson's Arm CPU).

# DeepStream inference (NVIDIA). `pyds` ships only inside the DeepStream
# SDK container/install (Linux + NVIDIA GPU + DeepStream SDK) -- not a pip
# package at all.

# Edge Impulse inference. `edge_impulse_linux` runs models already
# exported from Edge Impulse Studio (cloud, account required) -- there's
# no local "convert my own architecture" step for it to need here.
# edge-impulse-linux
